{
  "name": "CipherSQLStudio",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://7061262678.github.io/CipherSQLStudio",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "deploy": "gh-pages -d client/build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
